# Зависимости transport_tenders_parser.py. Все пакеты обязательны —
# run.bat устанавливает их автоматически при первом запуске.

# Загрузка страниц ЕИС/ТЭК-Торг, разбор HTML, выгрузка в Excel
requests>=2.31
beautifulsoup4>=4.12
openpyxl>=3.1

# Чтение PDF-документации закупок (ТЗ, спецификации). Дополнение [crypto]
# нужно для PDF «с защитой от копирования» (AES-шифрование пустым паролем).
pypdf[crypto]>=4.0

# Обход антибот-защиты ТЭК-Торг через установленный Яндекс Браузер /
# Microsoft Edge / Google Chrome (подключение по CDP). Отдельно скачивать
# браузер Playwright не нужно.
playwright>=1.40
